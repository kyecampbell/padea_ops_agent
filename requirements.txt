# Padea Operations Agent — Python dependencies
# Pin versions once the stack is stable.

# --- Claude API ---
anthropic>=0.40.0           # Claude Sonnet 4.6 tool-calling

# --- Database ---
psycopg[binary]>=3.1.0      # PostgreSQL adapter for Supabase (psycopg v3)
sqlalchemy>=2.0.0           # ORM / query builder

# --- Configuration ---
python-dotenv>=1.0.0        # Load .env into os.environ
pyyaml>=6.0.0               # Load agent_context/runtime_config.yaml

# --- Data validation ---
pydantic>=2.0.0             # Type-safe tool inputs/outputs
pydantic-settings>=2.0.0    # Config management

# --- Gmail API ---
google-api-python-client>=2.0.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.0.0

# --- Source data ingest ---
openpyxl>=3.1.0             # Read .xlsx files (students, sessions, caterers)
pdfplumber>=0.9.0           # Extract text from PDFs (menus, contacts, absences)

# --- Geospatial (caterer reach) ---
geopy>=2.4.0                # Postcode-to-coordinate lookup for RFP targeting

# --- Templating (decision log + emails) ---
jinja2>=3.1.0               # HTML decision log + email body rendering

# --- Utilities ---
httpx>=0.27.0               # Async HTTP (if needed for external lookups)
tenacity>=8.0.0             # Retry logic for Gmail API calls

# --- Testing ---
pytest>=8.0.0
pytest-asyncio>=0.23.0
